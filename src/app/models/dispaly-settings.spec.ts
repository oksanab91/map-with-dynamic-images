import { DispalySettingsFilter } from './dispaly-settings';

describe('DyspalySettings', () => {
  it('should create an instance', () => {
    expect(new DispalySettingsFilter()).toBeTruthy();
  });
});
